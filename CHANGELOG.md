# 変更履歴

このファイルは [Keep a Changelog](https://keepachangelog.com/ja/1.1.0/) 形式に従い、バージョンは [セマンティックバージョニング](https://semver.org/lang/ja/) を採用する。

## [2.1.0] — 2026-08-19

### 追加

- `BASE_PATH` を渡すと、その接頭辞の下にぶら下がって動く。旧 Flask 版と同じ 8080 を
  分け合うために要る。画面の中の行き先も転送先も自動更新の取得先も cookie の適用範囲も
  接頭辞の下に収まる。接頭辞を落とすと隣の実装へ飛んでしまうため、生成した HTML に
  素の行き先が残っていないことをテストで確かめている。
- `deploy-express.sh`。Node を入れ、Express 版を別ポートへ置く。旧版が使っている
  データベースをそのまま見せる。8080 には触れないので、動いているものは止まらない。
  流す前にデータベースの控えを取る。
- `switch-front.sh`。前段に nginx を置き、8080 を `/` の Flask と `/ex` の Express で
  分ける。受け取りに失敗したら 8080 を Flask に返す。`--revert` で手動の逆戻し。

### 直した

- 転送をまたぐ知らせの合図を `:` で分解していたため、`19:00` のような時刻が途中で
  切れて文面が出なかった。
- 末尾にスラッシュを付けた経路が通っていた。Flask は 404 を返すので、そちらに揃えた。
- cookie を読むとき、後から現れたものを採っていた。旧版が `/` に置いた cookie が
  `/ex` のものを上書きしうるため、先に現れたものを採る。ブラウザは細かい適用範囲を
  先に送るため、これで意図した側が残る。

## [2.0.0] — 2026-08-19

Flask から Express へ移した。Raspberry Pi の実行環境を Node に一本化するため。
データベースのファイル、スキーマ、URL、API の契約は変えていない。Python 版は
切り替えが済むまで `app/` と `tests/` に残す。

### 変更

- 実行環境が Node 20 以上になった。実行時の依存は `express` と `ejs` と
  `better-sqlite3` の 3 つ。テストは Node 組み込みの `node:test` で書き、
  追加の依存を持たない。
- 時刻を `+09:00` の固定オフセットとして扱う。日本は 1951 年を最後に夏時間を
  持たないため、タイムゾーンのデータベースを引かずに `zoneinfo` と同じ結果が出る。
  暦日は `YYYY-MM-DD` の文字列のまま持ち、`Date` の壁時計計算に頼らない。
- 転送をまたぐ知らせを、署名付き session ではなく合図だけの cookie で運ぶ。
  文面はサーバ側の表から引くため、cookie を書き換えても任意の文字列は画面に出ない。
  秘密鍵のファイルが要らなくなった。
- 名簿の操作は `node roster.js`。

### 差異

移行にあたって受け入れた違い。応答の意味は変えていない。

- 経路や method を間違えたときの `detail` を日本語にした。`error` は同じ。
- 転送の本文が HTML の案内から平文になった。`Location` は同じ。
- `now` の秒未満を出さない。`deadline` と `starts_at` は常に分単位なので変わらない。

### 確かめたこと

- 移植した 86 件のテストが通る。同じ枠へ 10 スレッドから同時に押して成立が 1 件
  だけになることも、worker_threads で確かめている。
- 純関数の出力を Python 版と 20,230 行にわたって突き合わせ、完全に一致した。
  滞在日の判定は 1 分刻みで期間全体を、枠の可否は 30 分刻みで全ての日・区分・
  浴室・枠を走査している。
- 2 つのサーバを同時に立てて 75 件の応答を比較し、上の差異を除いてすべて一致した。
  読み取りは 1 つのデータベースを共有させ、並走時と同じ形にしている。

## [1.1.0] — 2026-08-19

### 追加

- `/api/v1/` に JSON API。Slack bot から集計を読み、投票と予約もできる。認証は設けない。締切や先着順の判定は画面と同じ規則を通るため、二重に実装していない。
- 失敗は状態コードと機械可読な `error` 文字列で返す。画面の日本語文言とは分離してあるので、文面を変えても外部の実装は壊れない。
- 投票と予約が成功したときは、更新後の集計をそのまま返す。呼ぶ側が読み直さずに済む。
- 風呂の枠に `reservable` を持たせた。期間内でも受け付けるのは今日と明日だけなので、`state` が `free` でも予約できない枠がある。空き表示だけを見て予約に進むと拒否される。
- API の経路では、経路や method を間違えたときも HTML ではなく JSON を返す。HTML が返ると呼ぶ側の解釈が落ちて原因が分からなくなる。

## [1.0.0] — 2026-08-19

`furo-gohan` から `kominka-reserver` へ改名した。`deploy.sh` は旧名の配置を見つけると、データベースと設定を引き継いでから切り替える。

インターンの共同生活（2026-08-17 の晩から 2026-08-28 の朝まで）で実際に使うために作った初版。Raspberry Pi 5 に載せて運用している。

### 追加

- 風呂タブ。浴槽なし 15 分、浴槽付き 20 分の枠を先着順で予約する。朝は 06:00 から 08:00、夜は 19:00 から 25:00 開始まで。予約できるのは今日と明日で、1 人が持てるのは同じ日の朝に 1 枠、夜に 1 枠まで。取り消しはいつでもできる。
- ごはんタブ。白米の必要量を「通常」0.5 合と「大盛」1 合の 2 択で集め、合計の合数を出す。締切は朝の分が前日 22:00、晩の分が当日 18:00 で、受付開始の下限は設けない。
- 名簿。パスワードなしで名前を選び、端末が Cookie で覚える。画面からは編集できず、追加と非表示は `roster.py` で行う。
- `deploy.sh`。SSH でファイルを送り、仮想環境の作成から systemd への登録、起動確認までを一度に行う。何度流しても結果は変わらず、データベースと設定ファイルは上書きしない。

### 設計上の判断

- 先着順と 1 人 1 枠は SQLite の一意制約に守らせた。アプリケーション側で空きを確認してから書き込む形にすると、確認と書き込みの間に別の要求が割り込む。同じ枠へ 10 スレッドから同時に要求を出し、成立が 1 件だけになることをテストで確かめている。
- 時刻に依存する判定は現在時刻を引数で受け取る純粋関数に閉じ込め、データベースに触れないようにした。`clock.now()` を差し替えるだけでテストから時刻を固定できる。
- 「今日」を暦の日付ではなく滞在の一晩として扱う。夜の枠が翌 01:20 まで伸びるため、05:00 より前は前夜の続きとする。0 時を回っても、まだ始まっていない前夜の枠を取れる。境目を 05:00 に置いたのは朝風呂の開始 06:00 より前だからである。
- 操作はすべて通常のフォーム送信で行い、書き込み後は転送して再送信を防ぐ。JavaScript を切っていても予約と投票ができる。動く環境では 10 秒ごとに表の断片だけを取得して差し替える。
- 画面に文章を置かない。使い方の説明も動作の断り書きも書かず、出すのは見出し、時刻、名前、人数、ボタン、操作に失敗したときのエラーだけとする。
